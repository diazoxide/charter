# launch-freshness
PR: https://github.com/diazoxide/charter/pull/525
Branch: frame-launch-freshness-512

## Blocking

### 1

(B) WEAKER THAN origin/main — `charter/frame/state.py:486`. `workspace_for` returns `for_session(fid) or frame_workspace(fid) or resolve()`, which ranks the per-session pointer ABOVE `$CHARTER_WORKSPACE`; `workspace.resolve` ranks the env var above every pointer. A frame launched with the pin (charter's own documented mechanism for parallel/unattended agents — `hooks.py:3531` tells operators to `re-launch with CHARTER_WORKSPACE=<name> set`) plus an in-frame `charter workspace use other` now draws a workspace no command in the session acts on. Reproduced through the real `set_active` path on an isolated plane: with `$CHARTER_WORKSPACE=zeta`, `.charter/sessions/pinfid.workspace` = `other` (written by `charter workspace use other`, which charter allows and warns about), and `state.record_workspace(fid,'zeta')` — `MAIN TOP:  ⬢ zeta*` vs `BRANCH TOP:  ⬢ other*`, while `workspace.resolve()` answers `zeta` and `workspace.source()` answers `$CHARTER_WORKSPACE` in both. The `*` in `slots.py:238` means "$CHARTER_WORKSPACE pinned this", so the branch renders a name the env did not pin with the marker that says it did — self-contradictory, not merely stale. main is correct here. Fix is local: consult `$CHARTER_WORKSPACE` before rung 1, e.g. `env = os.environ.get('CHARTER_WORKSPACE'); if env and ws_mod.valid_name(env.strip()): return env.strip()` ahead of the existing chain, and pin it with a test (no existing test covers pin + session pointer — I confirmed by adding the case).

### 2

(B) SUITE ISOLATION, weaker than origin/main — `tests/test_frame_tmux_integration.py:3594`, `WindowInsideAnOperatorsTmux.test_nothing_of_the_operators_tmux_is_written_by_a_whole_launch`. It calls the real `commands_frame.cmd_launch`, which now forks, and it is NOT under `_no_real_detached_child`. The report claims "Both launch helpers and three ad-hoc call sites now stub `util.detach_self`"; this call site was missed. Verified by logging inside `util.detach_self` and running the test alone: `argv=[... '-P', '-m', 'charter', 'frame-gather', '--session', 'demo-64938', '--workspace', 'demo'] rc=1 err=b'No module named charter' PP=None cwd=<the checkout>`. The child inherits the developer's environment with no `CHARTER_ROOT` (`PersonaIso` redirects config IN-PROCESS only, and `tests/_planeguard.py` says it cannot see a subprocess), so on any machine where `sys.executable` can import charter — an editable install, a venv — it resolves the plane from the checkout cwd and runs `gather.refresh('demo-<pid>', workspace='demo')` + `state.bump` against the developer's LIVE plane, plus a git sweep and `glstate.maybe_spawn` over their real `workspaces/demo`. On `origin/main` no launcher test forked at all. Fix: add `_no_real_detached_child([])` to that test's `with` stack (or to the `_run_launch` context at line 3589), same as the other three ad-hoc sites.

