# timing-and-hygiene
PR: https://github.com/diazoxide/charter/pull/541 — open, NOT merged. Lists Closes #494, Closes #507, Closes #531. 29 files, +954/-321. No version bump, no stamping, no tag. News entry: docs/news/unreleased-a-green-tmux-run-now-means-something.md
Branch: phase0-timing-and-hygiene (worktree branch `worktree-wf_089de741-baf-3`, pushed to `origin/phase0-timing-and-hygiene`); commit 0903ebc; worktree /Users/aharon/IdeaProjects/charter/.claude/worktrees/wf_089de741-baf-3

## Blocking

### 1

The four environments do not agree, measured by me on my own extractions. Fresh/cleared: Ran 5621, OK. Used-plane/cleared: Ran 5621, OK. Inside a live frame (CHARTER_SESSION_ID + TMUX + TMUX_PANE): FAILED (15) — test_piece_claim x3, test_piece_history, test_piece_session_announce x3, test_session_identity x2, test_trace x3, test_workspace_lock, test_workspace_scope_honesty x2. CHARTER_WORKSPACE=anything: FAILED (8), byte-for-byte the implementer's list. I attributed both sets on the merge-base e1744f8 in the identical plane and cwd and got the same 15 and the same 8 — so this group neither caused nor hid them (they are #519/#521/#528) — but the brief's rule is unconditional and the approve criterion is that the suite's answer be environment-independent. It is not.

### 2

tests/test_frame_tmux_integration.py — MenuFormatIntegration.test_a_row_past_the_ninth_is_fired_by_no_keystroke_but_is_still_reachable was WEAKENED and no longer catches the regression it exists for. The `-` half went from `os.write(fd, b"-")` + `time.sleep(0.8)` + `assertFalse(os.path.exists(canary))` to `os.write(fd, b"-\x1b[B")` + `screen.await_more_drawn(mark)` + the same assertion. `await_more_drawn` returns the instant tmux paints anything (0-50 ms), but the effect it asserts absent is a `run-shell` spawning `sys.executable -c "open(canary,'w')"`, which I measured at ~0.7 s. Mutation `charter/frame/menu.py:_key` -> `return str(i + 1) if i < 9 else "-"` (the exact documented regression), with the test's own argv-shape line `assertEqual(cmd[10:][1::3][9:], ["", ""], cmd)` neutralised on BOTH sides to isolate the rendered-menu half: merge-base RED (`True is not false : `-` fired the tenth row — it is a real tmux key…`), this branch GREEN 5 runs out of 5. Restoring a wait before the assertion on this branch turns it RED again — so the assertion is right, the mutation still reaches it, and only the observation window is wrong. The test's docstring says "only a rendered menu can say which constant is right, which is the whole reason this class exists"; that property is gone and what is left duplicates test_frame_switch.py's constant pin. This is #494's own failure mode reintroduced one line lower — a readiness condition standing in for a settling condition. Fix: give the `-` half an observable that necessarily post-dates any row-10 execution (its own canary checked after the final selection lands, or spend `_DEADLINE` waiting for the canary and assert it never came).

