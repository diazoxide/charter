# ambient-environment
PR: https://github.com/diazoxide/charter/pull/540 (open, not merged). CI green on 3.11/3.12/3.13/3.14 — which also answers #519's open question about whether CI was only green because a GitHub runner exports none of these.
Branch: ambient-environment

## Blocking

### 1

charter's own legacy environment variables are neither scrubbed nor refused, and they still flip the suite. `charter/config.py:88` `_LEGACY_ENV_VARS = (("EDM_HOME", "CHARTER_HOME"), ("EDM_WORKSPACE", "CHARTER_WORKSPACE"), ("EDM_PERSONA", "CHARTER_PERSONA"))` is read by `_warn_legacy_env_vars()` at import of `charter.config`, which prints a 133-column stderr banner per name. None of the three matches `_PREFIXES = ("CHARTER_", "CLAUDE")` in `tests/_envguard.py:99` and none is in `_scrubbed_names()`, so they survive the scrub and are not in `_LOUD` either. REPRODUCED, full suite, branch tree, all CHARTER_*/CLAUDE*/TMUX/pane variables cleared, only the three legacy names exported: `Ran 5644 tests in 257.654s / FAILED (failures=2)` — `test_cli_smoke.TestInitDoesNotPrintOneEnormousLine.test_no_line_of_init_output_is_absurdly_wide` ('133 not less than or equal to 100 : widest init line is 133 cols') and `test_a_deny_survives_a_broken_channel.TestARealBrokenPipeRefuses.test_both_channels_dead_still_exits_blocking`. Same tree, same command, clean shell: `Ran 5644 … OK`. `EDM_WORKSPACE=legacy-ws` alone is sufficient; deterministic 2/2 on the two modules in isolation. Not a regression (main fails identically) but a gap in the class this PR claims to close, and it contradicts `tests/_envguard.py`'s own thesis that the guarded set is 'a property, not a list' — the file argues at length that `TMUX` is 'the one name spelled out … no constant to derive it from' while missing three names charter already keeps in a constant, and these are exactly the ones a long-time charter developer still has exported. Fix wrinkle to decide deliberately: `_scrubbed_names()` (tests/_envguard.py:102-116) must NOT import `charter.config` — that would resolve the plane ahead of the scrub, defeating the ordering `tests/__init__.py:39-44` exists to protect — so either spell the three names there with a comment citing `config._LEGACY_ENV_VARS`, or move the constant somewhere config-free; then pin the pair with a case in `tests/test_no_test_reads_the_operators_shell.py` asserting every legacy name in `config._LEGACY_ENV_VARS` satisfies `_envguard._in_namespace` (that module imports after install, so importing config there is safe).

