# vault-doctor
PR: https://github.com/diazoxide/charter/pull/493 (OPEN, MERGEABLE, base main) — pushed f5dde13 as HEAD:vault-doctor-470-471-472 --force-with-lease, rebased onto origin/main b4f135d (#499, #492 included).
Branch: vault-doctor-470-471-472
Weaker: None. Checked input by input, since `mkdir_for` replaces `mkdir(parents=True, exist_ok=True)` at four memstore sites. Outside the state directory the call is literally the same expression main used (`p.mkdir(parents=parents, exist_ok=True)`, parents defaulting True). Inside it, `private_mkdir` is strictly tighter and raises the same exception types main did: an existing directory is a no-op on both; a FILE in the way raises FileExistsError on both (private_mkdir re-raises after `is_dir()`, which is what exist_ok=True does and what every caller writing into the path depends on); a symlink to a directory is accepted by both (mkdir raises FileExistsError, `is_dir()` follows the link and returns); a parent that is a file raises NotADirectoryError on both. Every exception is an OSError subclass, so memstore's callers see nothing new. No `contain.writable` / `contain.dir_refusal` gate moved relative to its mkdir — all four still run before it. The scanner changed in the strict direction only: it flags a superset of what it flagged before (verified `scan_package()` is {} on the branch and names charter/memstore.py:76 the moment the bare mkdir goes back).

## Bypass

Yes — against the PR's own regression guard, not against the runtime fix. `tests/_statedirscan.py::_mkdir_sites` advertises `makedirs` coverage but reads the path expression from `fn.value` for attribute calls, so `os.makedirs(config.STATE_DIR / "x")` — the only spelling anyone writes — is scanned as the expression `'os'` and never flagged, in both `violations()` and `handed_violations()`. Injecting it into `charter/report.py` leaves `EveryStateWriterGoesThroughTheWalk` green; the identical `.mkdir` call fails it by file and line. No loose directory results today (charter/ contains zero `os.makedirs`), so this is the guard claiming coverage it does not have rather than #470 reproducing. Six-line fix verified green. The runtime defect itself did NOT reproduce under any spelling I tried: 45 commands and hooks x 2 umasks in fresh planes, five `$CHARTER_HOME` shapes (absolute, symlinked, `..`-containing, relative, four-levels-missing), and a pre-existing loose `.charter/` — all state directories charter created came out 0700, and no committed directory was tightened.

## Blocking

### 1

(C) tests/_statedirscan.py:232 `_mkdir_sites` — the advertised `makedirs` coverage is dead for the only spelling anyone writes. The filter accepts `name in ("mkdir", "makedirs")`, but for an attribute call it takes the path from `fn.value`, so `os.makedirs(config.STATE_DIR / 'x')` is scanned with path expression `'os'` and is never flagged. Verified: `violations()` returns `[]` and `handed_violations()` returns `{}` for it, while the identical `.mkdir` call and a bare `makedirs(...)` are both caught; injecting `os.makedirs(config.REPORTS_DIR / "x", exist_ok=True)` into charter/report.py leaves `EveryStateWriterGoesThroughTheWalk` GREEN, whereas the `.mkdir` form fails it by file and line. No live exposure (zero `os.makedirs` in charter/ today), so the news entry's claim is true as written — but the module title ("Which `mkdir` calls in `charter/`…"), the docstrings at lines 193 and 233 ("every ``mkdir``/``makedirs``"), and the test name `test_no_mkdir_in_the_package_can_make_a_loose_state_directory` all claim an enforcement that is not there. Fix (verified, suite stays green at 36/36): in `_mkdir_sites`, `if name == "makedirs" or not isinstance(fn, ast.Attribute): expr = ast.unparse(node.args[0]) (skip if no args) else: expr = ast.unparse(fn.value)`. Add the `os.makedirs` case to `TheScanSeesWhatItClaims` so it cannot rot back.

### 2

(C) docs/news/unreleased-one-row-per-persona-in-the-roster-reports.md — "Both renderers now map the roster through `contain.one_line` **before they measure their column widths**" and "both size their columns from the names" is true of `cmd_persona_list` (`nw = max(... len(s) for s in shown.values()) + 2`) and false of `cmd_persona_stats`, which prints through a fixed `f"{contain.one_line(r['persona']):<28}"` and measures nothing. Observable: a 35-character bounded name overflows the 28-wide PERSONA column on the branch exactly as it does on main. The bounding is correct; only the stated reason overreaches. Narrow the sentence to name `persona list` as the one that measures.

