# charter's provider seam, after #707/#708/#709: binding is by NAME agains

_2026-08-31 01:14 · persistent_

charter's provider seam, after #707/#708/#709: binding is by NAME against entry-point metadata (nothing imported); construction is by charter's own Component class (a deliberate hard charter-cp dependency, kept because __post_init__ is where provider-facing refusals land before a pane exists, with API_VERSION as the safety); the RECTANGLE is the committed [[frame.component]] table's and a provider may only declare Fixed(n) — Content()/Fill() are now refused, not coerced; and PAINT is the provider's own cells, of which the only charter markup that survives containment is ctx.chrome's seven roles, admitted by reading the SGR parameters numerically rather than matching the recipes' text.
