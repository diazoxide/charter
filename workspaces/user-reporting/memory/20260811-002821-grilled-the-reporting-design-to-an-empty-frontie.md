# Grilled the reporting design to an empty frontier (5 rounds). Settled: r

_2026-08-11 00:28 · persistent_

Grilled the reporting design to an empty frontier (5 rounds). Settled: reports file under the Reporter's own gh identity, no relay; draft and send are separate commands and the second IS the consent (no --yes, ever); crash detection attaches as a third except in cli.py:main where KeyboardInterrupt/ProcTimeout already define 'condition, not bug'; unknown subcommand is the gap signal; redaction is a six-field allowlist; filing is a flat gh call, deliberately not on the Forge protocol. Wrote ADRs 0001-0003 (committed docs tree) and spec.md (local). Spec NOT published - repo has only GitHub's default labels and setup-matt-pocock-skills was never run.
