# Regenerate the three SVG captures BEFORE 0.49.0 — captured.json is at 0.

_2026-08-22 12:57 · persistent_

Regenerate the three SVG captures BEFORE 0.49.0 — captured.json is at 0.47.1, so the next minor computes lag 2 and fails test_asset_freshness
