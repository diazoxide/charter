# Regenerate the three SVG captures before 0.50.0 — captured.json is at 0.

_2026-08-22 20:22 · persistent_

Regenerate the three SVG captures before 0.50.0 — captured.json is at 0.48.0, so the next MINOR computes lag 2 and fails test_asset_freshness (patches are unaffected)
