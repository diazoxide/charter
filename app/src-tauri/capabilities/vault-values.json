{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "vault-values",
  "description": "A vault's reveal and copy, the two commands that put a secret's value where the window can reach it, granted to the main window and to nothing else (ADR 0052). A capability of its own so that no window added to `default` later inherits them. `src/ipc.rs` fails if any other capability grants them, or if this one reaches past `main`.",
  "windows": [
    "main"
  ],
  "permissions": [
    "vault-values"
  ]
}
