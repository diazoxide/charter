{
  "$schema": "https://schema.tauri.app/config/2",
  "identifier": "dev.charter.app.e2e",
  "build": {
    "beforeBuildCommand": "npm run build:e2e"
  },
  "app": {
    "withGlobalTauri": true,
    "security": {
      "capabilities": [
        "default",
        "vault-values",
        {
          "identifier": "e2e",
          "description": "What WebdriverIO's plugins need, plus the window commands a scenario test drives the close-to-tray path with: `core:default` grants neither, and nothing the operator is given should be able to close or show the window from the page. Only a build made with this file and the `e2e` cargo feature has them.",
          "windows": [
            "main"
          ],
          "permissions": [
            "wdio:default",
            "wdio-webdriver:default",
            "core:window:allow-close",
            "core:window:allow-show",
            "core:window:allow-is-visible"
          ]
        }
      ]
    }
  }
}
