[package]
name = "stand-in"
description = "Writes a program a test is about to run, so the run never loses to ETXTBSY"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
publish = false

[dev-dependencies]
tempfile.workspace = true

[lints]
workspace = true
