[package]
name = "changelog"
version.workspace = true
description = "What each release of the app brought, read out of CHANGELOG.md for About Charter and the release body"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
publish = false

[dependencies]
# taiki-e's Keep a Changelog parser, the one `create-gh-release-action` uses. Only the library:
# its default features are its own CLI, and ours is `src/main.rs`.
parse-changelog = { version = "0.6", default-features = false }

[lints]
workspace = true
