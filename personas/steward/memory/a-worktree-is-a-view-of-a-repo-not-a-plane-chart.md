# A worktree is a view of a repo, not a plane. charter.toml is tracked, so

_2026-08-09 22:54 · persistent_

A worktree is a view of a repo, not a plane. charter.toml is tracked, so an embedded plane checks a copy into every worktree; plane identity therefore follows the MAIN working tree, or personas/vault/memory resolve into a directory git worktree remove deletes.
