# gh issue view / gh pr view print nothing under this agent sandbox (exit

_2026-09-10 15:27 · persistent_

gh issue view / gh pr view print nothing under this agent sandbox (exit 0, empty stdout, even redirected to a file). gh api works — read issues with 'gh api repos/<o>/<r>/issues/<n> --jq .body' and comments with the /comments endpoint.
