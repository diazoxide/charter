# Release 0.47.0 (2026-08-20) shipped #309+#310+#307 in ONE bump — the bat

_2026-08-20 16:06 · persistent_

Release 0.47.0 (2026-08-20) shipped #309+#310+#307 in ONE bump — the batching lesson applied deliberately, and it paid: preparing it surfaced the 'check: doctor' landmine that a per-fix release would have hit with a tag already burned. Version points that moved: pyproject.toml, charter/__init__.py, .claude-plugin/plugin.json, ELEVEN --plugin-version flags in hooks/hooks.json, and the docs/news/ entries (stamped by rename + version: rewrite). NOT moved: docs/assets/captured.json (asset-freshness gate fails only when lag EXCEEDS one minor, so 0.47.0 passes at lag exactly 1 — 0.48.0 WILL FAIL it until the SVG captures are regenerated) and the pin (this plane sets no [charter] version). Machine upgrade is two artifacts on two channels: 'charter update --to X' refuses to run inside a charter checkout (run it from anywhere else) and moves only the CLI; the plugin is the host's — 'claude plugin update charter@charter --scope project -y', and here it is PROJECT scope, not user, so the default-scope invocation fails with 'not installed at scope user'. Restart applies it.
