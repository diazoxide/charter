# The frightening half of a re-entrant news probe is not the loop: check:

_2026-08-21 12:17 · persistent_

The frightening half of a re-entrant news probe is not the loop: check: update reaches uv tool install in the process that IS the probe (depth 1, which the counter permits by design), so no env marker in a CHILD can stop it. Mutating commands must ask news.probing() themselves and call news.refuse_mutation() so the entry reports unknown, not pending — pending would invent a chore on a plane that already adopted the entry.
