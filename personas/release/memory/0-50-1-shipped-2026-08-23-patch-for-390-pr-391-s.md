# 0.50.1 SHIPPED (2026-08-23), patch for #390/PR #391 self-relaunch cwd sh

_2026-08-23 18:49 · persistent_

0.50.1 SHIPPED (2026-08-23), patch for #390/PR #391 self-relaunch cwd shadowing. hooks/hooks.json now carries ELEVEN --plugin-version flags (was nine when the charter was written, was 9 at 0.44.0) — never work from a remembered count, substitute globally then re-grep the OLD version across all four files. Merge of my own release PR (#392) SUCCEEDED via 'gh pr merge --squash' this time; the permission classifier that refused it on 0.50.0 did not fire. uv tool install --force --refresh charter-cp==0.50.1 succeeded first try because I had ALREADY polled https://pypi.org/simple/charter-cp/ until the wheel appeared (~15s after publish) before attempting it — poll the simple index BEFORE the install and the 'requirements are unsatisfiable' failure never happens.
