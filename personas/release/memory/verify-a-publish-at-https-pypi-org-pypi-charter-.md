# Verify a publish at https://pypi.org/pypi/charter-cp/<version>/json — th

_2026-08-09 22:54 · persistent_

Verify a publish at https://pypi.org/pypi/charter-cp/<version>/json — the project-level endpoint is CDN-cached and lags minutes, which reads as a failed publish when it is not.
