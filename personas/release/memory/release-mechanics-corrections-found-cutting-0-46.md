# Release mechanics corrections found cutting 0.46.0 (2026-08-19). (1) hoo

_2026-08-19 16:12 · persistent_

Release mechanics corrections found cutting 0.46.0 (2026-08-19). (1) hooks/hooks.json now carries ELEVEN --plugin-version commands, not six — the earlier memory's count is stale; never hand-count, just replace the version string and let TestVersionsMoveInLockstep confirm. (2) A minor bump can be BLOCKED by tests/test_asset_freshness.py: docs/assets/captured.json stamps demo.svg/personas.svg/statusline.svg, and the suite fails when a capture lags MORE THAN ONE MINOR. docs/assets/README.md says this is deliberate — 'exactly one release of slack'. Regeneration is fully scripted and stdlib-only: demo-plane.sh builds a throwaway plane, then the documented COLUMNS=150 statusline command and COLUMNS=96 ptyrun persona-list command, then capture-demo.sh <dir> <owner> <repo> piped to ansi2svg.py --animate with title 'nothing -> a working control plane' (pass the ORIGINAL title or the asset silently regresses), then social-card.py, then headless Chrome for the PNG. Renders must run the NEW version — put a shim on PATH that execs 'env PYTHONPATH=<clone> python3 -m charter', or the capture shows the installed version. At 0.44.1->0.46.0 only the rendered version string moved; demo.svg and personas.svg came back byte-identical.
