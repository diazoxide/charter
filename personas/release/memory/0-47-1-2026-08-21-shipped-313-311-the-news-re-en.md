# 0.47.1 (2026-08-21) shipped #313/#311, the news re-entrancy guard, as a

_2026-08-21 10:41 · persistent_

0.47.1 (2026-08-21) shipped #313/#311, the news re-entrancy guard, as a PATCH — both halves are fixes, no new flag/config key/changed default; the 'charter news --pending' output change only corrects a state that was already wrong (a green tick printed under the warning it contradicts). Verified the guard against real behaviour rather than the unit suite: the SAME 'check: doctor' entry planted in two scratch trees ran past a 45s cap on v0.47.0's news.py and answered in 1.33s with 'news: 1 unchecked' on 0.47.1, with 'news --pending' naming the entry and its check:. Reported unknown, not adopted. From 0.47.1 the stamp landmine costs an honest unknown instead of a hang — but the post-stamp bounded 'charter doctor' stays mandatory, because the guard bounds a self-referential check:, it does not make one CORRECT.
