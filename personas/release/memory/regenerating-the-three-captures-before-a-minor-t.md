# Regenerating the three captures before a minor (the asset-freshness gate

_2026-08-23 17:30 · persistent_

Regenerating the three captures before a minor (the asset-freshness gate): build a shim exporting THIS tree's charter onto PATH, then demo-plane.sh <dir> -> COLUMNS=150 statusline payload | ansi2svg --compose ... and COLUMNS=96 ptyrun charter persona list | ansi2svg; demo.svg needs capture-demo.sh <dir> diazoxide charter (real forge, read-only: init/discover/clone/status, gh account diazoxide). Cutting 0.50.0: only statusline.svg actually changed (its brand row prints the version, was still showing 0.48.0 while charter shipped 0.49.0 — the drift the gate exists to catch); personas.svg and demo.svg came back byte-identical, which is normal and not a sign the render failed. social-card.svg re-reads statusline.svg so it must be regenerated after, and its PNG can come out byte-identical because the version glyphs fall outside the crop.
