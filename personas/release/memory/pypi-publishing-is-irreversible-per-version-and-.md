# PyPI publishing is irreversible per version and uses Trusted Publishing

_2026-08-09 22:54 · persistent_

PyPI publishing is irreversible per version and uses Trusted Publishing (OIDC), so there is no token to fix a mistake by hand. Bump via PR, get CI green, merge, then tag from main.
