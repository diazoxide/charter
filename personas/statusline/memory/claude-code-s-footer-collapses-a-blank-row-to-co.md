# Claude Code's footer collapses a BLANK row to column 0, so a row that mu

_2026-08-09 22:54 · persistent_

Claude Code's footer collapses a BLANK row to column 0, so a row that must hold its column needs a visible glyph rather than leading spaces alone.
