# _TREE_WT (╰─) must stay textually distinct from _TREE_END (└─): render's

_2026-08-09 22:54 · persistent_

_TREE_WT (╰─) must stay textually distinct from _TREE_END (└─): render's 'tree keeps going' rewrite searches backwards for the last └─, and a child sharing that marker gets rewritten instead of its repo.
