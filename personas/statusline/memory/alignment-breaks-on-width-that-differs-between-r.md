# Alignment breaks on width that differs BETWEEN rows, not on width itself

_2026-08-09 22:54 · persistent_

Alignment breaks on width that differs BETWEEN rows, not on width itself. A glyph on every row (frame, divider) is safe; one on some rows (tree markers) is not; one on a header is worst — a header has no sibling row to reveal the drift.
