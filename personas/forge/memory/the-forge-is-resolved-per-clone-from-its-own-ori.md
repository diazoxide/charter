# The forge is resolved per CLONE from its own origin, not from the plane'

_2026-08-09 22:54 · persistent_

The forge is resolved per CLONE from its own origin, not from the plane's first [[forge]] block — a workspace can hold clones from different forges side by side.
