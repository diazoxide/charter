# Harness config-folder facts, measured 2026-09-11 on claude 2.1.268, code

_2026-09-11 12:00 · persistent_

Harness config-folder facts, measured 2026-09-11 on claude 2.1.268, codex-cli 0.147.0, opencode 1.18.23 in throwaway folders with no login. (1) Claude Code: CLAUDE_CONFIG_DIR moves the login (Keychain service becomes Claude Code-credentials-<sha256(dir)[:8]>), and charter's plugin is absent there even in a cwd whose project settings enable it, because the charter marketplace is declared only in ~/.claude/settings.json. The binary also reads CLAUDE_SECURESTORAGE_CONFIG_DIR, which by its code moves only the login (unmeasured at runtime). (2) Codex: CODEX_HOME moves config.toml, auth.json, the plugin cache and hook trust; codex plugin list cannot tell an empty home from a wired one. Its default shell_environment_policy passed an inherited *_TOKEN variable through (codex sandbox path). (3) opencode: the login is XDG_DATA_HOME/opencode/auth.json, plugins are XDG_CONFIG_HOME/opencode/plugin, and it also reads ~/.opencode/ as config regardless; moving XDG_CONFIG_HOME also logs gh out. (4) claude plugin list --json writes .claude.json and backups/ into whichever config folder it runs against, so charter doctor is not write-free. Detail: plane workspaces/harness-profiles/workspace.md, issue #969.
