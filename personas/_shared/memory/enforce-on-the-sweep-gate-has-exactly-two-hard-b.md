# --enforce ON THE SWEEP GATE HAS EXACTLY TWO HARD BLOCKERS, and the rende

_2026-08-29 03:33 · persistent_

--enforce ON THE SWEEP GATE HAS EXACTLY TWO HARD BLOCKERS, and the renderer's debt is NOT one of them (I got this wrong; the gate's author corrected me). The gate is scoped to lines a branch ADDS — it never charges a branch for lines it did not write — so main's standing debt (the report renderer, 106 of 168 mutations surviving) is stage B, a number, explicitly off stage C's critical path. The real blockers: (1) #632 — an annotation string under PEP 563 cannot be pinned by ANY test, and there is deliberately no suppression list (#370's reasoning), so --enforce would block a branch WITH NO REMEDY AVAILABLE TO ITS AUTHOR, which is how a gate becomes the thing people disable; (2) the sharded gate has never run a SHARDED workload on CI — every run so far had zero mutations under charter/, so plan has only ever emitted one shard, and SHARD_FIXED/SECONDS_A_MUTATION are arithmetic rather than measurement. A third worth deciding rather than discovering: under --enforce, 'unresolved' exits 3, so a mutation the runner could not measure blocks the branch — machine load blocking a merge. Strongest argument FOR: eight findings in the gate's own code and seven were holes in TESTS, not bugs in code.
