# TMUX IS DOING FOUR JOBS FOR CHARTER, NOT TWO — I ASSERTED TWO AND A PROT

_2026-09-07 11:19 · persistent_

TMUX IS DOING FOUR JOBS FOR CHARTER, NOT TWO — I ASSERTED TWO AND A PROTOTYPE REFUTED IT. I twice told the operator tmux does process supervision and compositing, and that only the rendering half was the complaint, so a non-tmux surface could render while tmux held the harness PTYs invisibly. Measured (docs/prototypes/ui-seam/): it also owns INPUT and SCROLLBACK, and neither separates from compositing. Input is termios-deep — frame/events.py clears ICANON/ECHO, sets VMIN/VTIME, writes the focus-report escape and negotiates mouse modes 1000/1002/1003/1006 against the OUTER terminal, which reports only from the active pane; F2/F12 are tmux key-table binds. Surface owns the keyboard means tmuxs binds are unreachable; tmux owns it means the surface gets no clicks. Scrollback IS copy-mode (frame-transcript). And the harness is a full-screen ANSI TUI, so showing it inside another surface means embedding a terminal emulator — that IS compositing, moved rather than removed, plus an IPC boundary. The coherent narrow version: tmux keeps the PTY and scrollback, a surface renders ONLY the readouts and never shows the harness — a control panel beside a terminal, not a replacement.
