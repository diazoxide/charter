# codex-cli 0.147.0 hook payloads, measured 2026-09-11 against a local moc

_2026-09-11 12:23 · persistent_

codex-cli 0.147.0 hook payloads, measured 2026-09-11 against a local mock Responses API with a throwaway CODEX_HOME and HOME (scratchpad handoff-4, --dangerously-bypass-hook-trust). Hooks fired from $CODEX_HOME/hooks.json (Claude Code's JSON shape); a project .codex/hooks.json in the untrusted work dir did not fire. A main-conversation PreToolUse payload carried NO agent_id under codex exec's default, -c approval_policy on-request, untrusted and never, -s workspace-write, --dangerously-bypass-approvals-and-sandbox, --approve-for-me and --enable multi_agent_v2 (including the payloads for its own spawn_agent and wait_agent calls). A sub-agent spawned through multi_agent_v2's collaboration namespace (function_call name spawn_agent with namespace collaboration; the dotted name collaboration.spawn_agent is 'unsupported call') sent agent_id and agent_type 'default' on its Bash call. codex exec sent permission_mode bypassPermissions under every approval policy tried; --approve-for-me sent default. The shell tool reaches the hook as tool_name Bash with tool_input.command; multi_agent v1's tools are deferred behind tool_search.
