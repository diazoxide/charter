# THE SWEEP DETECTS MASKED SURVIVORS AND SAYS SO — READ THE COLLECT JOB, D

_2026-09-02 21:29 · persistent_

THE SWEEP DETECTS MASKED SURVIVORS AND SAYS SO — READ THE COLLECT JOB, DON'T REDISCOVER IT. Its collect job emits: 'Another survivor shares this function. Two guards in sequence each look equivalent alone, so neither is pinned and they are read together.' Two worked examples from #822: (a) a '.gitmodules' stat masked a return-code guard — the ONE state measured as producing rc!=0 never reached git at all because the stat returned first, so the test exercised the failing-git path with EMPTY stdout where dropping the guard changes nothing; reaching it needed '.gitmodules' present PLUS a second unmapped gitlink, where git prints a mapped line AND THEN exits 128. (b) two conjuncts masked each other: dropping 'path.endswith(")")' alone only matters for a path containing ' (' but not ending ')', and dropping '" (" in path' alone only for the reverse — no fixture had either. Rule: when the sweep names two survivors in one function, do NOT pin them one at a time; find the state that reaches the SECOND one with the first satisfied, then verify by dropping each guard separately and checking exactly which test reddens.
