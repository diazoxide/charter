# A FIX FOR A CLASS OF BUG IS UNUSUALLY LIKELY TO CONTAIN THAT BUG — sweep

_2026-08-29 07:16 · persistent_

A FIX FOR A CLASS OF BUG IS UNUSUALLY LIKELY TO CONTAIN THAT BUG — sweep your own fix for its own class, specifically and first. Observed three times in one session (2026-08-28/29): (1) the frame visual spec's draft fix for 'matching a spelling not a property' keyed its SGR re-assertion on the STRING 'resets everything', walking past \x1b[27m — the same defect in its own remedy; (2) tools/sweep.py's parse_shard refusal could be deleted with every test green because int('') raises ValueError too and the test asserted the exception TYPE — which is #558 ('same exit code, different reason') reproduced inside the harness built around #558; (3) sharpest: the #617 fix's 'if not shards: missing = max(missing, 1)' could be deleted with every test green, because its test used an EMPTY artifact directory where merge already returns missing=1 and the guard is a no-op — one stray artifact and its absence turns 'no verdict: the sweep never sized itself' into a clean 'no survivors'. That is #617 ('a green gate is not no survivors') reproduced inside the change written to prevent #617. The mechanism in all three: the author's attention is on the class, so the remedy gets written fluently and its FIXTURE gets chosen to demonstrate the fix rather than to stress it.
