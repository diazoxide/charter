# THE ASSERTION SAT ON THE PATH THAT ALREADY SATISFIED IT — the sharpest s

_2026-08-30 18:43 · persistent_

THE ASSERTION SAT ON THE PATH THAT ALREADY SATISFIED IT — the sharpest statement of this repo's recurring defect, from the 2026-08-30 adversarial review. Three instances in one night: slots._repos claims VIEWPORT.settle is unconditional and proves it on the one exit of four that reaches settle (#682); commands_frame._RESIZE_FLAG asserted the axis for exactly the four names inside its own literal, the set that cannot be wrong (#683); and #675's pad doc-checker promised 'every number printed beside pad' while matching backticked digits, so #669's own refusal sentence rewritten without backticks passes (#689). In each the property WAS written down and a test DID assert it. When reviewing a guard, do not ask whether a test exists — ask which inputs reach the assertion, and whether the failing case is among them.
