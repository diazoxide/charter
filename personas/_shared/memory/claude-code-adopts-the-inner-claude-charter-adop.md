# CLAUDE CODE ADOPTS THE INNER .claude/, CHARTER ADOPTS THE OUTER PLANE —

_2026-09-03 14:05 · persistent_

CLAUDE CODE ADOPTS THE INNER .claude/, CHARTER ADOPTS THE OUTER PLANE — the two disagree by design and it is invisible until the files diverge. Measured at workspaces/harness-wrapper/charter (a clone of charter itself, carrying its own charter.toml/.claude/skills/personas): config.ROOT resolves OUT to /Users/aharon/IdeaProjects/charter because root._outermost hops out through the enclosing plane's workspaces/ (#200), while Claude Code's project settings are cwd-only (URt('projectSettings',cwd)=ce(cwd)) so the INNER .claude/settings.json and .claude/agents/ are what the session actually uses. Today the two settings files happen to be byte-identical, so nothing surfaces it; a divergence in that committed file is what would. RELATED CLAUDE CODE FACTS, read off binary 2.1.259: project settings/agents/skills/commands are CWD-ONLY with no ancestor walk; only CLAUDE.md walks up; TRUST is inherited up to the GIT ROOT (so workspaces/<ws>/ inherits the plane's trust, but every clone and worktree needs its own click); and TRUST GATES HOOK EXECUTION AND StatusLine GLOBALLY regardless of which settings source declared them — cJ() takes no source argument — so an untrusted directory loads the plugin, agents and skills and runs ZERO hooks.
