# A MARKER WRITTEN AND NEVER READ IS A FEATURE THAT LOOKS DONE. charter ha

_2026-09-08 12:15 · persistent_

A MARKER WRITTEN AND NEVER READ IS A FEATURE THAT LOOKS DONE. charter has written state.record_closed since #796, and until #929/#930 exactly ONE place ever read it: leave.plan. chats._by_workspace — the single walk that of_workspace, counts_by_workspace and touched_by_workspace all share — never asked was_closed. So closing a chat genuinely worked (confirmation, Enter, record_closed, kill-window, window gone) and the TAB STAYED ON THE STRIP, and pressing it answered "cannot switch: chat has no window any more". The operator reported this as "close is not closing and breaking entire flow", which is exactly right from where they were standing. It predated the affordance they blamed: F2 close had the same bug, and #925 merely put close on the strip so the eye landed on the one surface that never updated. Two lessons. First: grep every marker for its readers — a write with one reader is a claim only one code path believes. Second, and it needed a second fix: with the scan corrected a SIBLING strip still drew the dead tab seven seconds later, because a panel repaints on a state.version bump and nothing bumped its siblings — the same unshipped root as #829s liveness item.
