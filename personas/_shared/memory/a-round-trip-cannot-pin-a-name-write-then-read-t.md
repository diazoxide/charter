# A ROUND TRIP CANNOT PIN A NAME — write-then-read tests are green under a

_2026-09-01 22:29 · persistent_

A ROUND TRIP CANNOT PIN A NAME — write-then-read tests are green under any rename, so every on-disk name they touch is unmeasured. Found 2026-09-01 on #796, seven survivors from one cause: every test wrote with reopen.write and read with reopen.read, so renaming the manifest file, a suffix, a sidecar filename or ANY JSON key moved both sides together and stayed green. The mutations that survived were retune-string on "reopen.json", ".transcript", "cwd", "closed", "at", plus a retune-constant on a version int. WHY IT IS WORSE THAN AN ORDINARY TEST GAP: an on-disk format that outlives the process is a COMPATIBILITY SURFACE. This manifest deliberately survives reap so it can be older than the charter reading it — so a renamed .transcript leaves files nothing will ever offer or collect, and a renamed cwd key loses every chat working directory on the next quit. A round trip proves the two halves agree with each other; it proves nothing about agreeing with what is already on disk. REMEDY: spell the literal a second time in the test, by hand, the way test_a_real_click_on_a_real_tab_bar_switches spells its own refusal sentence rather than importing it. The duplication IS the assertion. Same family as [[a-case-two-arms-can-both-satisfy-tests-neither-t]] — two things that move together cannot test each other — and as the fixture-that-never-lies shape from the same branch cluster 1.
