# A test whose expected value is computed from the thing under test cannot

_2026-09-12 21:59 · persistent_

A test whose expected value is computed from the thing under test cannot fail. The tell: the expectation mentions the code under test — assertEqual(err, MSG.format(...)), f"{slots._inset()}*alpha", assertEqual(d.parent, _tab_order_file().parent). Removing {ws} from the template, changing the inset, or moving both files leaves all three green. Fix: write the expected value out literally. This shape cost charter's chat-handoff plan four rounds across three PRs (a title cap, a brief-privacy assertion, a committed-todo body, an operator sentence). The exception that is NOT the trap: an indirection through a configurable collaborator — statusline.accent('ok') in a slots test — because spelling the SGR would pin an operator's [frame] accents and fail falsely; close its one hole (an empty value making every assertIn pass) with a literal beside it.
