# A negative test pins one conjunct only if its fixture makes the other co

_2026-09-11 14:57 · persistent_

A NEGATIVE TEST PINS ONE HALF OF A COMPOUND CONDITION ONLY IF ITS FIXTURE MAKES EVERY OTHER HALF TRUE (tools/sweep.py on PR 970 at 3624287, 2026-09-11: 48 mutations, 7 survived). The P2 clause in doctor.check_guard_seen is if src == SETTINGS and standing.elsewhere and <that folder's settings.json declares the hook>. My negative test ('a plugin sighting names no file') used a plugin sighting AND a settings.json that did not declare the hook, so dropping the src == SETTINGS conjunct stayed green: the file check still refused. The fix is to make the negative's fixture declare the hook, so only the conjunct under test can refuse. The same round showed the sweep's other mutation kinds finding real gaps. retune-string caught a hint text no test asserted. swap-synonym (startswith->endswith) caught tests that only used a bare '~' segment, which both spellings match. retune-string on a dict key ('source') caught a branch reached only by a plugin-launched sighting with no harness name. Before calling a survivor 'equivalent', build the fixture where that exact half is the only thing deciding.
