# Claude Code subagents (2.1.267, seen twice on 2026-09-11): the stream wa

_2026-09-11 10:15 · persistent_

Claude Code subagents (2.1.267, seen twice on 2026-09-11): the stream watchdog KILLS a subagent when one tool call produces no output for 600 seconds. The task-notification reads 'Agent stalled: no progress for 600s (stream watchdog did not recover)', status failed. Both victims were reviewers running long silent foreground work: a 50-run real-tmux loop under CPU load, and a long regression battery. What a controller does: (1) before resuming, check that nothing the agent started is still running (a CPU-load loop left alive skews other agents' tests) and that no permission it removed stays stripped (read-only files under .git/objects are normal); (2) resume the SAME agent with SendMessage, which accepted the resume and keeps its context, telling it not to redo measurements it already saved; (3) put the rule in every reviewer brief up front: any command that can pass about 4 minutes runs with run_in_background writing to a file, is polled with short calls (sleep 60 then tail), and loops are split into chunks of at most 10 iterations per call. Related: a subagent that pauses on its OWN background command resumes by itself (see the subagent self-resume memory).
