# Claude Code 2.1.268 config folder resolution (read off the binary, measu

_2026-09-11 12:18 · persistent_

Claude Code 2.1.268's config-folder rules, read off the binary (~/.local/share/claude/versions/<v>, a bun bundle: search it with python re over the bytes; ugrep chokes on wide -o windows, and macOS has no timeout) and measured 2026-09-11 for #969. CONFIG HOME = (CLAUDE_CONFIG_DIR ?? join(homedir(), '.claude')).normalize('NFC'). It is ??, so CLAUDE_CONFIG_DIR='' is KEPT and names the cwd: measured, 'CLAUDE_CONFIG_DIR= claude plugin list --json' listed ./plugins/installed_plugins.json. USER SETTINGS = <home>/settings.json, or cowork_settings.json under CLAUDE_CODE_USE_COWORK_PLUGINS. PLUGIN MANIFEST = <dir>/installed_plugins.json, where <dir> is CLAUDE_CODE_PLUGIN_CACHE_DIR if set, else <home>/plugins (cowork_plugins under the cowork switch). .claude.json = <home>/.config.json when that exists, else join(CLAUDE_CONFIG_DIR || homedir(), '.claude'+suffix+'.json'). That is || and the raw value (not NFC), and suffix is '-custom-oauth' when CLAUDE_CODE_CUSTOM_OAUTH_URL is set. Measured: with the var set, claude writes .claude.json inside that folder. The Keychain item gains -<sha256(dir)[:8]> only when CLAUDE_CONFIG_DIR is set. charter mirrors the CLAUDE_CONFIG_DIR half in charter/harness/claude_code.config_home and global_config_file (PR for #969). The cache-dir, cowork and oauth-suffix knobs are NOT followed.
