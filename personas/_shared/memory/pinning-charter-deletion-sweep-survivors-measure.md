# Pinning charter deletion-sweep survivors, measured 2026-09-12 on PR #981

_2026-09-12 08:01 · persistent_

Pinning charter deletion-sweep survivors, measured 2026-09-12 on PR #981. (1) An uncontain survivor on a value a shape regex already validated (profiles.NAME_RE for a profile name) is pinned by LENGTH, not escapes: contain.readable clips at DISPLAY_LIMIT=160 and appends three dots, and NAME_RE bounds shape only — a 200-char declared name reddens every such site. (2) A containment test on a value round-tripped through a state file must use ESC (\x1b), not CR: Path.read_text() translates \r to \n, so the escape you assert on changes under you. (3) A drop-if survivor on the stdin-isatty guard is NOT pinned by patching sys.stdin.isatty — tests/_ttyguard replaces sys.stdin with /dev/null, whose readline() returns empty at once, so a missing guard looks identical. Patch sys.stdin wholesale with a stub whose readline() raises. (4) A shift-boundary survivor on a monotonic deadline IS pinnable: patch time.monotonic with an iterator whose second tick equals the deadline exactly, and assert the third tick is left unread. (5) Dropping that same conjunct makes the loop infinite, so bound the sleep stand-in (raise past N calls) or the test hangs instead of failing.
